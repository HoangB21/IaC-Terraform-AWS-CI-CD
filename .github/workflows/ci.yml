# Workflow for CI
name: Terraform CI

on:
  pull_request:
    branches:
      - staging
    paths:
      - 'environments/stg/**'
      - 'environments/prd/**'
      - '!modules/**'
      
permissions:
  contents: read
  pull-requests: write # allow comment on PR
  
jobs:
  terraform-ci:
    runs-on: self-hosted  # EC2 runner 
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0   # lấy đủ history
      
      - name: Fetch develop & staging
        run: |
          git fetch origin develop
          git fetch origin staging
      
      - name: Find changed directories
        id: changed-dirs
        run: |
          CHANGED_DIRS=$(git diff --name-only origin/staging origin/develop \
            | grep '^environments/' \
            | cut -d/ -f1-4 | sort -u)
          echo "dirs=$CHANGED_DIRS" >> $GITHUB_OUTPUT

      - name: Terraform Validate + Plan
        id: tf
        run: |
          RESULT=""
          for dir in ${{ steps.changed-dirs.outputs.dirs }}; do
            echo "---- Running in $dir ----"
            cd $dir
            terraform init -input=false -backend=false
            terraform fmt -check
            terraform validate
            terraform plan -input=false -no-color -out=tfplan || true
            terraform show -no-color tfplan > plan.txt
            PLAN=$(cat plan.txt)
            RESULT="$RESULT\n\n### $dir\n\`\`\`\n$PLAN\n\`\`\`"
            cd - >/dev/null
          done
          echo "result<<EOF" >> $GITHUB_OUTPUT
          echo -e "$RESULT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Comment plan on PR
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: terraform-plan
          message: |
            ## Terraform Plan Result
            ${{ steps.tf.outputs.result }}
